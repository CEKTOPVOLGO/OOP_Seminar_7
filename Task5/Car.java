/*
package Task5;

class Car {
    private Engine engine;
    private Color color;
    private Interior interior;

    public Car(CarFactory factory) {
        engine = factory.createEngine();
        color = factory.createColor();
        interior = factory.createInterior();
    }

    public void assembleCar() {
        System.out.println("Assembling car with " + engine.getDescription() + ", " + color.getColor() + " color, and " + interior.getDesign() + " interior.");
    }
}*/
